import React from 'react';
import s from './Blog.module.css'

const Blog = (props) => {
    return (
        <div className={s.content}>
            Blog
        </div>
    )
};

export default Blog;
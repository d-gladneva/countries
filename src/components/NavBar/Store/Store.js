import React from 'react';
import s from './Store.module.css'

const Store = (props) => {
    return (
        <div className={s.content}>
            Store
        </div>
    )
};

export default Store;
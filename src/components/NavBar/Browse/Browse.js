import React from 'react';
import s from './Browse.module.css'

const Browse = (props) => {
    return (
        <div className={s.content}>
            Browse
        </div>
    )
};

export default Browse;
import React from 'react';
import s from './Mobile.module.css'

const Mobile = (props) => {
    return (
        <div className={s.content}>
            Mobile
        </div>
    )
};

export default Mobile;